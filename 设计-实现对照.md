# 万象牧语 · 设计-实现对照矩阵

> 对照《万象牧语-设计汇总.md》各节；状态图例：
> ✅纯 = 纯规则层已实现并有断言覆盖（可离线 javac 验证）
> ✅桩 = MC 注册层已写且通过"API 桩"编译校验（仍需真实构建确认）
> ⏳ = 待接 MC 实体/事件（Phase 2）
> 📋 = 规划（Phase 3/4）
> 🚫 = 有意搁置（后续版本）

| 设计章节 | 规则要点 | 实现位置 | 状态 |
|---|---|---|---|
| §1 属性系统 | 好感0-200/心情0-100(初70)/饱食0-20/默契0-100/速度档 | `core/ModConstants`、`core/state/MonsterGirlState` | ✅纯 |
| §1 元素标签 | 炽寒序乱光暗魂（label 先行，数值延后） | `core/taxonomy/Taxonomy.ElementFamilia` | ✅纯（数值🚫） |
| §2 状态机档位 | 野生/友善/伙伴/誓约；进档看数值、退档规则、黏性伙伴 | `core/taxonomy/RelationshipStage`、`core/rule/RelationshipRules` | ✅纯 |
| §2 好感增减 | 投喂+1/送礼+2/摸头+1；攻击-1/冷落衰减/离线×0.1/饱食低-1 | `core/rule/InteractionRules`、`RelationshipRules` | ✅纯 |
| §2 每日上限 | 玩家×魔物娘×行为×游戏日 一次 | `core/interaction/DailyInteractionTracker` | ✅纯 |
| §2 伙伴休眠 | 双0+24h 无互动→休眠；饥饿例外；绑玩家摸头唤醒 | `core/rule/DormancyClock` | ✅纯 |
| §3 心情 | 活动不足判定（60 采样/16格/3天）、饱食≤6 -1 | `core/rule/MoodActivityClock`、`ModConstants` | ✅纯 |
| §3 狂暴 vs 低落 | 狂暴=特定条件无差别（**同种族敌意是物种变量**）；低落=心情<20 模板化 | `core/behavior/MoodLowTemplateRegistry`、`NestGuard`、`OrdoTemplates` | ✅纯 |
| §3 低落·随机攻击 | 强制游荡、只打近身非玩家、击杀1-2min回30 | `core/rule/LowMoodRecoveryTracker` | ✅纯 |
| §4 饱食进食 | 饥饿伤害/锁血免疫/休眠饥饿例外、自给(固定需巢、自然无中生有)、伙伴需管理 | `core/rule/HungerRules` | ✅纯（伙伴宝箱取食=⏳AI） |
| §5 战败/急救 | 非野生倒地、60s锁血免疫、锁血后急救箱(10%+6饱食)、脆弱期补刀致死 | `core/rule/KnockdownRules` | ✅纯 |
| §6 背包/快捷栏 | 快捷栏(主手/副手/口部/附肢N)+背包；只用手持槽 | `core/species/SpeciesProfile`(槽位数据) + `core/behavior/InventoryRules` + `core/gui/GirlInventoryLayout` + `menu/GirlInventoryMenu` + `client/GirlInventoryScreen` | ✅**容器 GUI 已实装**（拾取/武器等行为规则仍待接） |
| §6 拾取/武器/宝箱存取 | 5s入包、武器伤害对比/≤10%耐久回收、伙伴存入伙伴宝箱 | `core/behavior/InventoryBehaviorRules` + `GirlInventory(区间操作)` + `MonsterGirlEntity(tickLiftToBackpack/tickEat/tickWeapon/tickChestStorage)` + `block/CompanionChestBlock(+BlockEntity)` + `menu/CompanionChestMenu` + `client/CompanionChestScreen/ChestRenderer` | ✅**已实装**（模型暂借末影箱） |
| §7 互动操作表 | 摸头/投喂/送礼/丢食/命令/初稿GUI/百晓镜/陌生人限制 | `InteractionRules`、`CommandRules` | ✅纯（MC 事件=⏳） |
| §8 誓约 | 好感200+协议书、1对1、默契占位、残忍仪式解除 | `RelationshipRules.tryVow`、`MonsterGirlState.vowed` | ✅纯（道具/仪式=📋） |
| §9 命缕/命帛/复活 | 死亡必掉/记录档案/不可毁/闪电充能+绽放之刺复活；命帛收纳 | 物品已注册占位 | ✅桩（机制=📋 Phase 3） |
| §10 物品 | 初稿/誊写台/百晓镜/礼物盒/急救箱/誓约协议书/伙伴宝箱/绽放之刺/巢心 | `LUIds`+`LURegistries`+lang+占位模型贴图 | ✅桩（行为=📋） |
| §10 图鉴/初稿 | 目击→SIGHT+BASIC；缩放→TAXONOMY+HOSTILITY；喜爱互动→PREFERENCES | `core/encyclopedia/*` | ✅纯（GUI=📋） |
| §11 阿拉克涅 | 巢心领地/威胁/红线/狂暴/伙伴豁免；**守巢威胁内敌视一切生物**、**被惹立刻反击**、**同种族敌意两变量**（巢穴威胁内/狂暴是否攻击同族，阿拉克涅=均 true）；槽位4+9 | `core/behavior/NestGuard`、`BuiltinSpecies` | ✅纯（实体 AI=⏳/Phase 4） |
| §12 猫又 | 礼物表(25%×4，绿宝石特例)、伪装、偷鱼5min、亡灵视野+1 | `core/loot/WeightedGiftTable`、`BuiltinSpecies` | ✅纯（行为 AI=⏳/Phase 4） |
| §13 分类学模板 | 六阶元类目注册表（增删/附属模组扩展）+ 纲行为模板 | `core/taxonomy/Taxonomy`、`core/behavior/OrdoTemplates` | ✅纯（类目注册/模板复用有断言） |
| §14 动画预留 | 15 通用+个别追加；分层复合；猫又基准 | 资源位预留（文档） | 📋 待美术 |
| §15 技术要点 | 服务端权威/模板驱动/爬墙二期/配置项 | `core/*` + build 工程 | 结构✅桩 |
| §16 术语·ID | id 常量集中 | `registry/LUIds` | ✅桩 |
| §17 待定/延后 | 元素数值延后、混沌域占位、"寒"=aqua 已定 | Taxonomy.AQUA.element="aqua" | 已定/🚫 |

## 冒烟断言统计（tools/rules-smoke，仅需 JDK）
- 已覆盖：互动/档位/衰减/休眠/心情/战败急救/图鉴/礼物表/命令游荡/低落恢复/快照/巢心守卫/饱食自给/物种档案完整性/战斗面板数值/敌意目标选择（模板驱动 HostilityRules+OrdoTemplates）/威胁以巢心为圆心/**守巢威胁敌视范围内一切生物（玩家/亡灵/普通生物一视同仁）**/**被惹反击无视巢距、豁免绑玩家**/无巢狂暴开关（刷怪蛋豁免）/**同种族敌意两变量（巢穴威胁内与狂暴时是否攻击同族魔物娘；模板默认+物种覆盖）**/**物种专属能力参数与状态机（蛛网/蛛丝拉拽/毒牙/流血/暗影箭/闪避：参数、冷却门控、流血叠加与周期结算、弹道种类与伤害结算口径、弹道寿命上限）**/**巢穴方块物种变量（有巢声明/无巢显式 NEST_NONE 与 hasNest 总开关）**/**伪装形态规则（物种变量/三类现形触发/静默恢复/伙伴档失效）**/**巢穴结构几何（树干收分/巢壁球壳/出入口 20% 球冠/巢心方位/蛛网递减与通路/螺旋楼梯）**/**猫又社交习性（偷鱼限制与背包槽位/村民绿宝石概率与数量/睡醒赠礼选择/陪睡条件/亡灵视野每日心情）**/**自然生成（刷猫转换概率与"未声明即不转换"/自然生成原因口径/自然消失与获得好感后转持久/好感标记粘性）**/**猫形伪装表现（待机坐姿、休眠/陪睡趴姿、原版过渡步长与钳制、原版全黑猫贴图口径）**/**头部跟随视角（角度最短弧包装/部分 tick 插值/偏航与俯仰限幅）**/**动画名与骨骼名对表（动画资源存在性 + AllHead 骨骼存在性）**/**动画规则（main 优先级排序与各状态取值、悬空判定、`random_` 系列名归一、同系列播放期间互斥、3%/秒判定与到点未中不触发、`pet` 触发名匹配）**/分类学类目注册增删/纲模板复用/全阶元模板默认特性（TaxonTemplates+TemplateKeys）/物种覆盖优先级/GirlState 与图鉴编解码往返/**野生投喂认主（好感≥10 自动绑定喂食者）**/**双档移速与倍率热改（跟随 >8 格加速、追到跟随最低距离 6 格才降回 walk、攻击即最快、物种覆盖 > 全局、未写回落档案值、默认配置文件写出各物种倍率现值、档位倍率选择 `speedScale`）**/**快捷栏与背包（槽位切分与命名、背包格数夹取 0..27、GUI 行数与逐行居中、面板高度、槽位唯一且都在面板内）**/**物品栏行为（拾取门/5 秒入包/进食阈值与咀嚼时长/武器面板伤害与耐久 ≤10%/伙伴宝箱搜索 8 格/存取 2 格与"背包满才存"）** —— **280 项，ALL_PASS**
- 运行：见 README「规则冒烟验证」

## MC 层校验（三档）
- ① **真实构建**：`gradlew.bat build --offline` → `BUILD SUCCESSFUL`（含 GeckoLib 依赖；产物在 `build/libs/`）
- ② **对真实 NeoForge API 的全工程离线编译**（`tools/compile-full-offline.ps1`，classpath 由 `tools/dump-classpath.gradle` 从 Gradle 导出 = 补丁版 MC + NeoForge 26.2.0.77 + 运行库 + GeckoLib）：`FULL OFFLINE COMPILE OK (real NeoForge API)`（**无桩**，NeoForge 事件/补丁签名问题在此暴露）
- ③ API 桩校验（仅在无真实 jar 时）：`MC-LAYER STUB COMPILE OK` / `-Mode stub`

## MC 接线（实体/互动）当前状态

| 功能 | 位置 | 状态 |
|---|---|---|
| 实体注册/属性/占位渲染（monster_girl / arakne / nekomata 三实体类型共用 MonsterGirlEntity） | `LUEntities`、`MonsterGirlEntity`、`client/*` | ✅同上 |
| 刷怪蛋：阿拉克涅 arakne_spawn_egg / 猫又 nekomata_spawn_egg（生成即野生档案状态，同自然生成） | `LURegistries` + `LUEntities` + `MonsterGirlEntity.ofSpecies` | ✅同上 |
| 状态挂载 + NBT 持久化（GirlStatePersistence/Base64） | `MonsterGirlEntity` | ✅同上 |
| 互动：Shift+右键命令循环（跟随/待机/游荡；待机=站立；跟随=朝绑玩家寻路+>32格就近传送） | `MonsterGirlEntity.mobInteract` + `tickFollow` | ✅同上 |
| 互动：投喂（**野生=认主起点**：未绑定个体被喂 +1 好感/日，好感≥10 自动绑定该玩家为友善对象；已绑定=绑玩家本人 +1/日、回饱食；他人=只保当天不掉，不给好感；每日上限随档持久化） | `mobInteract` + `PersistentDailyLimiter` | ✅同上 |
| 互动：送礼（潜行+右键非食物非工具；喜爱 +2/日，每日一次） | `mobInteract` + `InteractionRules` | ✅同上 |
| 互动：摸头（友善+ +1 好感/+10 心情，每日一次；**主手与副手都必须为空**才触发，任一手持物即不摸头，避免"边用物品边摸头"）；休眠唤醒（同样要求双空手） | `mobInteract` + `InteractionRules` | ✅同上 |
| 互动去抖：**所有右键交互统一原生防重**——客户端 mobInteract 镜像服务端分支并返回 CONSUME（一次点按只发一个交互包），服务端另有 0.1s 兜底窗口；含 Shift+右键命令/送礼、投喂、礼物盒、急救、百晓镜、调试件 | `MonsterGirlEntity.clientSideInteract` + `allowInteract` + `ModConstants.INTERACT_DEBOUNCE_TICKS` | ✅同上 |
| 互动：礼物盒（主手盒+副手物→右键空气打包；右键魔物娘送礼，喜爱 +2/日） | `PresentCaseItem` + `mobInteract` | ✅同上 |
| 观测：百晓镜右键→目击+缩放解锁图鉴条目；喜爱投喂/送礼→解锁"喜好" | `ServerCodex` + `mobInteract` | ✅同上（图鉴内存态，持久化待附件） |
| 急救箱（倒地锁血结束后使用） | `mobInteract` + `KnockdownRules` | ✅同上 |
| 面向玩家：每 tick 持续看向身旁绑玩家（LookControl 每 tick 驱动，避免"看一眼就回正"抽搐） | `MonsterGirlEntity.tickLookAtOwner` | ✅同上 |
| 物种专属敌意（骨架）：纯规则目标选择（野生/友善/伙伴、阿拉克涅**守巢威胁内敌视一切生物**（玩家/亡灵/普通生物一视同仁、仅豁免绑玩家）、猫又拟态被动+亡灵狩猎+被惹反击、低落只打近战非玩家、倒地/休眠/伙伴不战、**被惹反击无视巢距但豁免绑玩家**、狂暴不分敌我；**同种族敌意由两物种变量控制**（AI 层按 speciesId 过滤，见实体行）） | `core/behavior/HostilityRules` | ✅纯（35 断言） |
| 近战攻击接入：索敌扫描 + 近战伤害取 SpeciesProfile 数值（亡灵 ×倍率）+ 受击反击记忆（**被惹立即设为目标，100 tick 记忆内重扫不丢弃**）+ 低落击杀回心情；**创造/旁观玩家不作目标**；**同种族敌意两变量在 AI 层过滤**（巢穴威胁内/狂暴是否攻击同族，读 `OrdoTemplates.nestThreatAttacksOwnKind/enragedAttacksOwnKind`） | `MonsterGirlEntity.tickCombat/pickCombatTarget/markProvoked/doMeleeAttack`、`GirlDamageHandler`、`GirlDeathHandler` | ✅桩编译 |
| **物种专属能力（Phase 4a-①）**：蛛网（减速2+挖掘疲劳2/5s，5s 冷却）、蛛丝拉拽（10 伤 + 拉向发射者，15s 冷却）、近战毒牙（中毒2/8s）、流血（层级+1 无上限、命中刷新 10s、每 2s 结算=层级）、暗影箭（≥8 格、10 伤魔法、10s 冷却）、弹射物闪避（5s 冷却 + 免疫窗口 + 侧/后位移）。**三种远程能力均以「弹道」发射**：自研投射物实体 `lu_bolt`（无重力直线、不拾取、超时消失，视觉先复用原版箭贴图）；**弹道 ≠ 原版弹射物**——暗影箭按魔法伤害结算（不受弹射物保护附魔影响），蛛丝按弹射物伤害结算，蛛网不造成伤害（`BoltKind.dealsProjectileDamage()`）。能力参数全部走物种档案（`SpeciesAbilities` + `TemplateKeys.ABILITY_*`，参数 0 = 无该能力） | `core/behavior/SpeciesAbilities` + `entity/LuBoltEntity` + `client/LuBoltRenderer` + `MonsterGirlEntity.tickAbilities/doMeleeAttack/tryDodgeProjectile` + `GirlDamageHandler` | ✅纯（参数/冷却/流血/弹道口径有断言）+ 桩编译 |
| **外生息门·巢穴附近增益**：领地内周期获得生命恢复/力量/速度/抗性（4s 刷新、6s 持续），离开领地自然到期 | `MonsterGirlEntity.tickNestBuff` + `TemplateKeys.FLAG_CLASSIS_NEST_BUFF` | ✅桩编译 |
| **伪装形态（Phase 4a-② 起）**：物种变量 `species.disguise`；猫又野生/友善默认**猫形**，空手右键/受伤/主动攻击（含发射弹道）→ 现人形，人形静默 60s → 恢复猫形，投喂不现形，伙伴档伪装习性失效；形态为**同步数据**（客户端换模型/贴图/影子）并随存档持久化，猫形碰撞箱 0.6×0.7。**猫形渲染 = 原版猫模型 + 原版全黑猫贴图（`cat_all_black`）+ 原版猫动画**（`client/CatFormRenderer` 复用原版 `AdultCatModel`，通用动作直接取本体、猫专属字段按姿态填充；趴姿过渡步长沿用原版 `Cat` 数值，旋转位移复刻原版 `CatRenderer#setupRotations`）；人形渲染 = GeckoLib（`client/NekomataRenderer` 按形态分派） | `core/behavior/DisguiseRules` + `core/anim/CatFormRules` + `MonsterGirlEntity(defineSynchedData/revealHumanForm/tickDisguise/getDefaultDimensions)` + `client/NekomataRenderer` + `client/CatFormRenderer` | ✅纯（规则有断言）+ **对真实 API 编译通过** |
| **客户端表现同步（动画/姿态输入）**：动画控制器与渲染只在客户端跑，而休眠/命令/进食/跳跃计时只在服务端 —— 因此新增同步数据 `DATA_DORMANT` / `DATA_SLEEPING_WITH_OWNER` / `DATA_ANIM_JUMP` / `DATA_ANIM_EAT` / `DATA_OBEY_MODE`（服务端每 tick 写、仅在变化时发包），主控制器与猫形姿态一律读同步值。**此前 `main_eat`/`main_jump`/`main_sit` 在客户端取不到状态，实际不会播放（本次修复）** | `MonsterGirlEntity(defineSynchedData/syncVisualState/isDormantVisual/isStandingByVisual/isJumpAnimActive/isEatAnimActive)` | ✅同上（编译 + 断言） |
| **巢穴结构（程序化生成，无 NBT）**：黑森林 `dark_forest` 中按设计建造——深色橡木巨树（树干 15 / 总高 25、自下而上收分）、树顶分岔数枝、分岔处**空心不规则球型蛛丝巢**（羊毛；水平半径 5 / 垂直半径 3.5）、一侧约 **20% 球面开口**作出入口、入口反方向的巢壁内安置**蜘蛛巢心方块**、巢内蜘蛛网自巢心向外**密度递减**且巢心朝入口留**通路**、树干外表面刻**螺旋楼梯凹陷**（与出入口方位对齐）；形状数学在纯层 `core/worldgen/NestShapeRules`（可断言），建造在 `worldgen/ArakneNestFeature`（Feature `lingua_universalis:arakne_nest`），区域限定经 `neoforge:add_features` 生物群系修饰符 | `core/worldgen/NestShapeRules` + `worldgen/ArakneNestFeature` + `LURegistries.FEATURES` + `data/lingua_universalis/{worldgen,neoforge}` | ✅纯（几何断言）+ 桩编译 |
| **巢穴住户锚点**：蜘蛛巢心方块带方块实体 —— 区块加载后确认半径 24 内是否已有阿拉克涅；无则在巢心上方生成一只（`EntitySpawnReason.STRUCTURE`）、设为不掉落刷新、并**认领该巢心**；标记持久化 → **一个巢心只生成一只**（死亡后不补生成，复活走命缕链路） | `block/CubileAraneaeBlock(Entity)` + `LURegistries.CUBILE_ARANEAE_BE` | ✅桩编译 |
| **猫又社交习性（Phase 4a-③）**：偷鱼（5 分钟/次、**仅游荡状态**、只偷玩家**背包 9–35 槽**的鱼、偷后跑远 8 格吃掉不囤积）、村庄偷绿宝石（每天 50%、1–3 颗）、**睡醒赠礼**（成功偷窃后玩家下次睡醒送出；礼物表各 25%，偷到绿宝石则必送绿宝石且一次性）、**猫形陪睡**（伙伴非待机 + 绑玩家睡觉且在 8 格内 → 猫形态，醒后恢复人形）、**亡灵视野心情**（幽暗目默认：视野内出现亡灵每天一次 +1 心情）。全部走物种变量/阶元默认（`SocialHabitRules`，非猫又物种自动跳过） | `core/behavior/SocialHabitRules` + `MonsterGirlEntity.tickSocialHabits` + `TaxonTemplates(UMBRA)` + `BuiltinSpecies(nekomata)` | ✅纯（规则有断言）+ 桩编译 |
| **猫又 GeckoLib 模型/动画（Phase 4a-④）**：GeckoLib **5.5.5**（Modrinth Maven，**用版本 id `fTK5ltWI` 精确锁定 NeoForge 版**——同版本号还有 fabric/forge 文件，用版本号会拿到 Fabric jar：编译能过但运行期 FML 判定"geckolib is not installed"；详见 README 的依赖坑说明。mods.toml 声明为**必装依赖**；**外置依赖、未内嵌 jar-in-jar** → 玩家侧需自行安装 GeckoLib，仓库内附 `libs/geckolib-neoforge-26.2-5.5.5.jar`）；资源 `assets/lingua_universalis/geckolib/models/nekomata.geo.json`（**美术侧导出的逐面 UV 版本**，167 cube 全逐面——GeckoLib 的箱型 UV 会把 cube 尺寸 `Mth.floor` 取整导致含小数的 cube 错位，故不使用箱型 UV；备用转换器 `tools/boxuv-to-faceuv`）、`assets/lingua_universalis/geckolib/animations/nekomata.animation.json`（动画名与 `AnimationRules.REQUIRED_ANIMATIONS` 由冒烟测试对表，改一名即报警）、`textures/entity/nekomata.png`；**GeckoLib 只扫描 `geckolib/models` 与 `geckolib/animations`，且键是剥掉目录+后缀的裸名字**（模型与动画的 Identifier 都是 `lingua_universalis:nekomata`），放错目录会 `Loaded 0 models` 并渲染成紫黑占位；**动画规则**（纯层 `core/anim/AnimationRules`，有断言）：`main_` 前缀=主要动画（互斥 + GeckoLib 过渡 4 tick）、其余=混合叠加（`constant`/`tail_swing` 常驻，`pet` 由摸头触发的一次性动画）、`random_` 前缀=每秒 3% 触发某系列（同系列播放期间互斥、不同系列可并行；系列名忽略结尾数字）、`main_jump` 仅主动跳跃触发（播一遍停末帧 → 自动 `main_airborne`）、`main_airborne` 悬空优先于 walk/run/sprint、游泳/潜行暂用 walk；`playing1`/`hiss1` 暂不启用 | `core/anim/AnimationRules` + `MonsterGirlEntity(implements GeoEntity + registerControllers)` + `client/NekomataGeoModel` + `client/NekomataRenderer` | ✅纯（动画规则断言）+ **对真实 NeoForge/GeckoLib API 编译通过** |
| **头部跟随视角 + 动画层级修正（Phase 4a-④ 追加）**：①`AllHead` 骨骼的**旋转**跟随原版转头（偏航 = 头−身、限幅 ±75°，俯仰限幅 ±60°，只动旋转不动位置/缩放），纯规则 `HeadLookRules` + 渲染器 `extractRenderState`/`adjustModelBonesForRender` 叠加；②**控制器顺序即优先级**（GeckoLib 后写覆盖先写）：`constant` 若排在 `main` 之后会覆盖其对手臂的旋转 → 实为「leftarm/rightarm 不随动画摆动」的真因，已调整为 `constant → main → tail → pet → random_*` | `core/anim/HeadLookRules` + `client/NekomataRenderer` + `MonsterGirlEntity.registerControllers` | ✅纯（角度包装/插值/限幅断言）+ **对真实 API 编译通过** |
| **体型/视线（Phase 4a-④ 追加）**：碰撞箱走**物种变量**（`species.bodyWidth` / `species.bodyHeight` / `species.eyeHeight`，未声明回落 0.6×1.8/1.62）；猫又按爬行姿态声明 **0.9 × 1.5**，视线高度取模型 `ViewLocator` 骨骼枢轴（34.47642 ÷ 16 = **2.1548**），并随 `model.renderScale` 缩放（模型实测约 3.2 格高，需要对齐碰撞箱时把缩放设小） | `core/species/BodyRules` + `TemplateKeys.SPECIES_BODY_*` + `BuiltinSpecies(nekomata)` + `MonsterGirlEntity#getDefaultDimensions` + `LUEntities` | ✅纯（体型三元组/缩放联动断言） |
| **本地可调设置**：`config/lingua_universalis.properties`（不存在自动生成）——头部跟随开关/符号/限幅、**主要动画过渡时长（设 0 = 不做过渡，规避"绕远路"旋转）**、模型渲染缩放；解析失败一律回落默认值 | `core/config/LuSettings` + `LinguaUniversalis`（启动读取）+ `NekomataRenderer`/`MonsterGirlEntity` | ✅纯（默认值/解析/兜底断言） |
| **动画触发修复（本轮）**：①**跳跃动画**原来只在 `isJumping() && onGround()` 触发——`isJumping()` 只有 AI 通过 `JumpControl` 请求跳跃时才为真，而脊索界自带"自动踏上 1 格"，寻路常常直接走上去 ⇒ 永不触发；改为"离地且向上运动（或 AI 请求跳跃）"判定（被动下坠不触发）；②**悬空动画**加"稳定"门槛（连续离地 4 tick 或掉落 >0.5 格才播 `main_airborne`）⇒ 进退游戏时不再先重置成 airborne 一两秒；③**摸头动画**本就是每次摸头都触发（调用点在每日好感上限判定之前，与上限无关），真正原因是 pet 控制器带 2 tick 过渡而该动画仅 0.17 秒（≈3 tick），过渡把姿态稀释掉了 ⇒ pet 控制器过渡改为 0 | `core/anim/AnimationRules(isActiveJump/shouldShowAirborne)` + `MonsterGirlEntity(tick/registerControllers)` | ✅纯（触发/稳定判据断言）+ **对真实 API 编译通过** |
| **目视目标（本轮）**：`tickLookAtOwner` 现在是"绑定玩家优先（12 格），否则注视 8 格内最近的玩家"（`headLook.lookAtNearbyPlayer` 可关）；走原版 `LookControl.setLookAt(实体)`，因此**偏航与俯仰都有来源**，头部跟随的俯仰分量随之生效 | `MonsterGirlEntity.tickLookAtOwner` + `LuSettings` | ✅纯（设置默认值断言） |
| **动画名/骨骼名对表（防静默失效）**：冒烟测试读取动画资源，逐一核对 `AnimationRules.REQUIRED_ANIMATIONS`（`random_blink` 改名 `random_blink1` 这类改名会静默不播）；并核对模型里存在头部跟随所需的 `AllHead` 骨骼 | `tools/rules-smoke/SmokeMain` + `AnimationRules.REQUIRED_ANIMATIONS` | ✅纯（资源对表断言） |
| **自然生成：村庄刷猫 → 猫又（Phase 4a-⑤）**：原版在 `#minecraft:cats_spawn_in`（村庄/沼泽小屋）**自然生成**一只猫时，按物种变量 `species.villageCatConversionChance` 投掷（猫又 = 50%、未声明 = 0 → 不转换），命中则取消这只猫的加入并**延迟一 tick 原地生成一只野生猫又**（读档不转换、刷怪蛋/指令/繁殖/刷怪笼不触发；物种由变量驱动而非硬编码）。**获得过一次好感后不再被自然刷新掉**：`everAffectioned` 粘性标记（随存档持久化）+ 绑定 → 转持久个体，防止养成中断 | `core/behavior/SpawnRules` + `event/GirlNaturalSpawnHandler`（`EntityJoinLevelEvent` + `EventHooks.finalizeMobSpawn`）+ `LUEntities.typeForSpecies` + `TemplateKeys.SPECIES_VILLAGE_CAT_CONVERSION` + `MonsterGirlEntity(everAffectioned)` | ✅纯（转换/持久判据断言）+ **对真实 NeoForge API 编译通过** |
| **双档移速（本轮修复）**：基础 `MOVEMENT_SPEED` 恒为 `0.25`，按档位乘倍率——攻击敌人 → 物种最快倍率；**跟随**绑玩家且距离 >8 格 → 最快；其余（游荡/野生/跟得够近）→ walk（所有物种默认 1.0）。**真因**：属性此前只在 `finalizeSpawn`/`setSpeciesId` 写过一次，而那时既无敌人也无绑定玩家 ⇒ 永远停在 walk 档，fast 永不生效；现在 `tickMovementSpeed()` **每 tick 重算**、仅变档时写属性（`MoveControl#tick` 每 tick 读 `speedModifier × 属性值`，故改属性下一 tick 即生效，无需重新寻路；也不必每 tick 发属性同步包）。**「有没有敌人」读本模组自己的 `combatTarget`**（实体从不调 `setTarget()`，`getTarget()` 恒为 null——同一误解还导致战斗中的饱食自愈照常触发、随机散步会抢追击路径，一并修正）；跟随判据是 **8 格加速 → 一路追到"跟随最低距离"6 格才降回 walk**（6 格同时也是停步距离，共用同一常量；中间 2 格是滞回带）。倍率可热改：`speed.walkScale`/`speed.fastScale`（物种覆盖 > 全局 > 档案值，`≤0` 视为不写） | `core/behavior/MovementSpeedRules(speedScale/useFastSpeed 滞回)` + `MonsterGirlEntity(tickMovementSpeed/applySpeciesMovementSpeed/hasCombatTarget)` + `LuSettings` | ✅纯（档位/滞回/倍率选择断言）+ **对真实 API 编译通过** |
| **魔物娘 GUI：快捷栏 + 背包（本轮）**：手持**初稿**右键**伙伴档**魔物娘打开容器界面（非伙伴不开放；已绑定的个体只允许绑玩家打开）。快捷栏 = 物种档案命名槽（猫又 `main_hand/off_hand`，阿拉克涅再加 `appendage1/appendage2`），背包 = `SpeciesProfile.backpackSize()`（猫又 8 / 阿拉克涅 9，夹在 0..27）；两者共用一个**连续容器**（`0..快捷栏数-1` 快捷栏、其后背包），槽位含义与 GUI 几何都在纯层（`InventoryRules` / `GirlInventoryLayout`）。**26.2 的 GUI API 已换代**：没有 `GuiGraphics`，屏幕改为 `extractBackground/extractLabels/extractRenderState(GuiGraphicsExtractor, …)`；`extractContents` 内部会 `pushMatrix + translate(leftPos, topPos)` ⇒ **背景用屏幕坐标、槽位/标签用面板内坐标**（差一次偏移就是双倍偏移）。`blit` 的 12 参重载是「源矩形 + 目标尺寸」，故面板用「上片段原尺寸 + 中段 1px 灰底纵向拉伸 + 下片段原尺寸」拼成任意高度（贴图暂时复用原版漏斗，槽位底图取该贴图里现成的 18×18 槽位）。她背包随存档持久化：**存「槽位索引数组 + 物品列表」**而非原版 `storeAsItemList`（后者读档会压紧到最前面、串位）；真死时 `spawnAtLocation` 撒落原地（倒地不掉） | `core/behavior/InventoryRules` + `core/gui/GirlInventoryLayout` + `entity/GirlInventory` + `menu/GirlInventoryMenu` + `client/GirlInventoryScreen` + `LURegistries.MENU_TYPES` + `LinguaUniversalisClient(RegisterMenuScreensEvent)` + `MonsterGirlEntity(girlInventory/openInventoryFor/mobInteract)` + `GirlDeathHandler(dropGirlInventory)` | ✅纯（槽位/布局断言）+ **对真实 API 编译通过** |
| **初稿：GeckoLib 3D 模型 + 最朴素的展示（本轮回退）**：初稿用美术的模型（`初稿.geo.json` / `初稿.png`）表达，不再是 2D 贴图。**26.2 的物品外观走 item model definition**：`assets/<ns>/items/<item>.json` 声明 `{"type":"minecraft:special","base":"<只放 display 的模型>","model":{"type":"geckolib:geckolib"}}`（GeckoLib 用 mixin 往 `SpecialModelRenderers` 注册 `geckolib:geckolib`），`models/item/first_draft.json` 只留 `gui_light`/`particle`/`display`。**动画与右键交互全部移除**：`FirstDraftItem#registerControllers` 有意留空 ⇒ 模型永远默认（合着）姿态，右键没有任何初稿自身行为（对魔物娘右键开她的 GUI 仍然保留，且仍限主手）；`BookAnimationRules` / `BookAnimationTracker` / 渲染器里的阶段注入与第三人称 180° 旋转都已删除。手持两个手位改用**原版普通物品**（`item/generated`）的 display 数值（左右手各自镜像），物品栏/掉落物/展示框仍是美术调好的数值。美术的开合/翻页动画（`初稿动画.json` 的 display/open/flip）留在资源目录备用，未接入 | `item/FirstDraftItem` + `client/{FirstDraftGeoModel,FirstDraftItemRenderer}` + `LURegistries.FIRST_DRAFT(registerItem)` + `MonsterGirlEntity(mobInteract 主手门禁)` + `assets/.../{items,models/item}/first_draft.json` | ✅ 编译通过（MC 层渲染需游戏内确认） |
| 巢心（领地巡游纲核心）：**巢穴方块为物种变量**。**启动期坑（已修）**：初稿原用 `ITEMS.register(…, () -> new FirstDraftItem(new Item.Properties()…))`，物品构造时 `Properties` 里还没有注册 id ⇒ 启动注册阶段直接 `NullPointerException: Item id not set`；改为 `ITEMS.registerItem(id, FirstDraftItem::new, p -> p.stacksTo(1))`（该重载先写入 id 再交给工厂）。**编译期查不出这类注册期语义错误，只能靠真启动** |
| 巢心（领地巡游纲核心）：**巢穴方块为物种变量**（`SpeciesProfile.nestBlockId` + `hasNest()`；无巢物种显式 `nestBlockNone()` = `NEST_NONE`）、巢心认领/持久化、威胁与领地以巢心方块为圆心（非本体）、狂暴评估、返巢；**无巢物种（猫又）不认领巢心、不返巢、不狂暴、不得巢穴增益**（读档忽略残留巢心数据） | `MonsterGirlEntity.tickNestState/findNestNearby/claimNest/isNestBlock` + `SpeciesProfile.hasNest` + `NestGuard` + `HostilityRules(distToNestSq)` | ✅同上 |
| 调试移除器（右键瞬间移除魔物娘） | `DEBUG_GIRL_REMOVER` + `mobInteract` | ✅同上 |
| 调试好感道具（右键 +10 好感 = `DEBUG_AFFECTION`；右键 +1 好感 = `DEBUG_AFFECTION_1`，均不限次数） | `DEBUG_AFFECTION(_1)` + `mobInteract` | ✅同上 |
| tick：饱食下降/饥饿伤害/心情活动不足采样/休眠计时 | `MonsterGirlEntity.tick` | ✅同上 |
| 战败倒地：致命伤→60s 锁血（免疫/清状态/浮水）；玩家攻击 -1 好感 | `GirlDamageHandler` | ✅同上 |
| 死亡必掉命缕 + 个体快照组件 girl_snapshot | `GirlDeathHandler` + `LURegistries.DATA_COMPONENTS` | ✅同上 |
| 命缕掉落强化：抗毁(EntityInvulnerabilityCheck) / 不消失 / 荧光；闪电充能 fatum_charged；/lurevive 按快照复活 | `GirlFilumProtectionHandler`、`GirlLightningHandler`、`LUCommands` | ✅同上 |
| 绑定后不再自然消失 | `MonsterGirlEntity.tick` | ✅同上 |
| 开发命令 /lubond info/bond/set/selftest | `LUCommands` | ✅同上 |

### 游戏内快速验收清单（本阶段已全部验收，归档）
```mcfunction
give @p lingua_universalis:cubile_araneae   // 放置巢心（领地巡游纲圆心）
give @p lingua_universalis:arakne_spawn_egg
give @p lingua_universalis:nekomata_spawn_egg
summon lingua_universalis:arakne
lubond @e[type=lingua_universalis:arakne,limit=1] info
lubond @e[type=lingua_universalis:arakne,limit=1] bond
give @p lingua_universalis:first_aid_kit
```

> ✅ **已全部验收（清单归档）**：
> 巢心守卫（守巢敌视一切生物 + 狂暴触发，含同种族敌意两变量：阿拉克涅威胁内/狂暴均攻击同族）/
> 右键交互单击防重（原生 CONSUME 机制 + 0.1s 兜底；**命令循环亦并入同一机制**，不再有独立 0.5s 窗口）/
> 被惹立刻反击 / 摸头（空手限定）/ 物种敌意近战骨架 / 猫又亡灵减伤 /
> 野生投喂认主（好感≥10 自动绑定喂食者）/
> 百晓镜显示绑定玩家（在线=玩家名，离线=offline:uuid 前 8 位，未绑=none）/
> 调试好感道具（`DEBUG_AFFECTION` +10 / `DEBUG_AFFECTION_1` +1，均不限次数）。
>
> 新功能上线后按同样格式追加验收项。

### 待验收（Phase 4a-① 物种专属能力 · 弹道版）
```
give @p lingua_universalis:arakne_spawn_egg
give @p lingua_universalis:nekomata_spawn_egg
give @p lingua_universalis:cubile_araneae
```
1. **阿拉克涅·蛛网**：与她对打 → 会看到**一枚箭向她飞向你**，命中后你被施加**减速 II + 挖掘疲劳 II**（各 5 秒），约每 5 秒一发；
2. **阿拉克涅·蛛丝拉拽**：站在 3~12 格外 → 约每 15 秒飞来一发，**命中瞬间**你被拉向她并受到约 10 点伤害（未命中则无效果）；
3. **阿拉克涅·毒牙**：被她近战命中 → 身上出现**中毒 II**（约 8 秒）；
4. **巢穴增益（外生息门）**：她待在巢心领地内时，身上持续有**生命恢复/力量/速度/抗性**（离开领地后到期消失）；
5. **猫又·流血**：被她近战命中 → 出现流血效果并**每 2 秒扣血**；连续命中会**叠加层级**（扣血更多）、刷新持续时间；
6. **猫又·暗影箭**：站在 8 格外 → 约每 10 秒飞来一发，命中为**魔法伤害**（约 10 点；对亡灵 ×5）；
7. **猫又·弹射物闪避**：用弓箭射她 → 约每 5 秒会被闪开一次（免伤 + 侧/后位移）；
8. **弹道口径验证**：装备**弹射物保护**附魔后被暗影箭命中 → 伤害**不应**被减免（魔法弹道不算弹射物）；被蛛丝命中 → 属弹射物伤害，**应**被减免；对飞行中的弹道按原版箭的观感（无重力直线，命中/超时即消失，不可拾取）；
9. **弹道射空自动消失**：让她向天空/空地发射（自己躲开弹道）→ 弹道应在 3~4 秒内自然消失、不留存、不插地、可拾取物掉落为 0；远离后回来看不到残留弹道；疯狂对射时实体数不增长（TAB/`/kill @e[type=lingua_universalis:lu_bolt]` 可验证当前数量）；
10. **巢穴变量**：猫又（`nestBlockNone`）**不会**认领蜘蛛巢心、不会返巢、不会因无巢狂暴、也不会获得巢穴增益；阿拉克涅仍以蜘蛛巢心为领地/威胁圆心；
11. **猫又伪装（猫形 ↔ 人形）**：野生/友善猫又生成时是**黑猫**形态（体型矮小、影子更小）；
    - 空手右键她 → **立刻现出人形**（同时结算摸头）；受伤 或 她主动攻击/发射暗影箭 → 现人形；**持食物投喂不现形**；
    - 人形后**静默约 60 秒** → 自动恢复猫形；退出重进游戏形态保持（存档持久化）；
    - `/lubond ... bond` 成为伙伴后不再自动伪装（人形常驻）；
12. **巢穴结构生成（黑森林；外观为占位，未来由你提供 NBT 替换——链路不变，只换外观实现）**：新开/未探索的黑森林中约每 90 个区块尝试一次 → 应能看到**深色橡木巨树**（约 25 格高，树干约 15 格、下粗上细），
    - 树干外表面有一条**螺旋上升的凹陷楼梯**，可沿它走到树顶（螺旋终点正对巢穴出入口）；
    - 树顶分岔处有**羊毛搭建的空心球形蛛丝巢**（水平半径约 5）；一侧有**出入口**开口，进入后内部有蜘蛛网（越靠巢心越密），
    - **蜘蛛巢心方块**在入口正对面的巢壁内，且巢心朝入口方向没有蛛网遮挡 → 站在入口就能直接点到巢心；
    - 区块加载后巢心上方会**自动出现一只阿拉克涅**并认领该巢心（她不会因区块卸载消失；挖掉巢心会让她进入狂暴；杀掉后巢心不再补生成，可用命缕/绽放之刺复活）。
13. **猫又社交习性**（需要一只**未绑定的野生/友善**猫又 + 游荡状态）：
    - **偷鱼**：把鱼放进你的**背包**（不是快捷栏），在她 8 格内等待约 5 分钟 → 她偷走 1 条并跑开约 8 格吃掉（会有一条提示；快捷栏/主副手/护甲的鱼不会被偷）；跟随/待命状态下不偷；
    - **村庄偷绿宝石**：带她靠近村民 → 每天 50% 概率偷 1–3 颗（隐蔽，无提示）；
    - **睡醒赠礼**：她成功偷窃后，你**睡一觉醒来** → 收到礼物（腐肉/骨头/铁锭/火药各 25%；若她当天偷到过绿宝石则必送绿宝石，且只送一次）；
    - **猫形陪睡**：成为伙伴（`/lubond ... bond` + 好感≥100）后，**非待机状态**下你躺床睡觉且她在 8 格内 → 她变回猫形态陪睡，你醒来后恢复人形；
    - **亡灵视野心情**：让僵尸/骷髅出现在她 16 格视野内 → 当天第一次 +1 心情（每天一次）。
14. **猫又动画（GeckoLib）**（`summon lingua_universalis:nekomata`，GeckoLib 5.5.5 为必装依赖）：
    - **站立**：静止时播放 `main_idle`；**移动**：走动时切到 `main_walk`，两段之间有约 4 tick 的过渡混合（不是硬切）；
    - **跳跃**：主动跳跃瞬间播一遍 `main_jump`（**停末帧**，减少落地穿模），播完自动进入 `main_airborne` 悬空循环；落地后回到走动/站立；
      （说明：从方块边缘**走下去**的自由落体不走跳跃动画，直接进 `main_airborne`）
    - **进食**：投喂成功 / 偷吃鱼时播放 `main_eat`；**休眠**（倒地休眠状态）播放 `main_sit`；
    - **摸头**：空手右键时叠加播放一次 `pet` 动画；
    - **随机动画**：每系列每秒 3% 判定——`random_blink`（眨眼）、`random_idle1`（小动作）；同一系列播放期间不会再次触发同系列，不同系列可同时发生；
    - **尾巴**：`tail_swing` 常驻循环叠加（不影响主体动作）；
    - **叠加层**：`constant` 常驻循环始终参与混合（呼吸/微动），叠加层不会打断 `main_` 动画；
    - **已知取舍**：游泳/潜行暂无专属动画，沿用 walk；`main_playing1` / `main_hiss1` 本轮暂未接线（摇尾玩物/威吓待后续接入）。
15. **自然生成（村庄刷猫 → 猫又）**：进入一个**村庄**（或沼泽小屋）等待原版刷猫——每次自然刷出猫时 **50%** 会直接变成一只**野生猫又**（黑猫形态、状态面板为野生）；
    - 刷怪蛋 / 指令 summon / 繁殖 / 刷怪笼产出的猫**不会**被转换（只认世界自然生成）；
    - 退出重进游戏不会重复转换（读档不判定）；
    - **获得过一次好感后**（摸头/投喂/送礼任意一次生效）她**不再被自然刷新掉**（远离也不会消失）；在此之前野生个体仍会像原版生物一样自然消失；
    - 阿拉克涅等其他物种不受影响（未声明该变量的物种概率为 0）。
16. **猫形伪装（原版猫模型 + 原版动画）**：让她处于猫形（野生/友善默认猫形；或人形后静默 60s 自动恢复），观察：
    - 外观为**原版全黑猫**（`cat_all_black` 贴图、无项圈），体型与猫形碰撞箱（0.6×0.7）观感一致，有原版猫的**走动摆腿**与尾巴摆动（动画取自她自身的移动，不是自研动画）；
    - `Shift+右键`切到**待机**命令 → 她**坐下**（原版猫坐姿）；切回跟随/游荡 → 站起；
    - 进入**休眠** → 她**趴下**（原版猫趴姿，过渡步长同原版）；唤醒后站起；
    - 以猫形陪睡（伙伴 + 你睡觉）→ 趴姿 + 原版"趴在睡觉玩家身上"的额外位移；
    - 空手右键 → 立刻现出人形并换成 GeckoLib 人形模型（两套渲染切换不残留、不闪烁）。

> 🚫 **已搁置待修（后续版本）**：Shift+右键命令循环（跟随/待机/游荡）的**行为本身**仍有缺陷（防重已并入原生机制；剩余问题为跟随/待机/传送/朝向等手感），修复后再回到验收清单。
## 待办（按依赖顺序；前序实体/规则项均已在你的真实构建中验收）
1. **修复搁置项**：Shift+右键命令循环（跟随/待机/游荡）的**行为缺陷**（防重已解决）——需你描述具体表现，之后重新进入验收清单；
2. Phase 3：物品/方块自定义行为（初稿 GUI/誊写台、百晓镜 HUD 与图鉴写入持久化、命缕掉落与命帛收纳、礼物盒打包、誓约协议书、伙伴宝箱容器）；
3. Phase 4a（无需美术）：物种专属行为接线——阿拉克涅吐丝/蛛丝拉拽、猫又偷鱼与礼物表赠送流程、自然世界生成（黑森林巢穴结构 ✅ / 村庄猫又 ✅）；
4. Phase 4b：其余物种的 GeckoLib 模型/动画与正式贴图 —— **需要美术**：§14 清单（猫又人形已接入；**猫又猫形需 `geo/nekomata_cat.geo.json` + `textures/entity/nekomata_cat.png`**，放入即自动启用；阿拉克涅仍为程序占位）。
5. **巢穴结构外观替换**：当前 `ArakneNestFeature` 为**程序化占位**；你建好结构后提供 NBT/结构文件 → 只需替换该 Feature 内部实现（或把 `placed_feature` 指向结构池），**Feature 注册 → placed_feature → 生物群系修饰符 → 巢心锚点/认领链路均不变**。
